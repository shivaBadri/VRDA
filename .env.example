# ─────────────────────────────────────────────────────────────
# Contact form email delivery
# Wire these into src/app/api/contact/route.ts (see the TODO there).
# Option A — SMTP (e.g. Resend SMTP, Gmail, Zoho, your host):
# SMTP_HOST=smtp.your-provider.com
# SMTP_PORT=587
# SMTP_USER=your-user
# SMTP_PASS=your-password
# CONTACT_TO=info@vrdaprime.com
#
# Option B — Resend API:
# RESEND_API_KEY=re_xxxxxxxx
# ─────────────────────────────────────────────────────────────

# Public site URL (used for metadata / canonical)
NEXT_PUBLIC_SITE_URL=https://www.vrdaprime.com
